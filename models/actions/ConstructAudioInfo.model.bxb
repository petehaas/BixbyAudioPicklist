action (ConstructAudioInfo) {
  type(Constructor)
  description (Build Audio input)
  collect {
    input (audioItem) {
      type (audioPlayer.AudioItem)
      min (Required) max (One)
    }
  }
  output (audioPlayer.AudioInfo)
}
